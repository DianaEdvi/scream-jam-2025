using UnityEngine;

public class RoomTraversalController : MonoBehaviour
{
    // This will be the boss of the player and moth and room objects
    // Essentially, it will be the one moving the player/mothman from room to room so that those guys dont manage themselves 
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    
}
